<?php
/**
 * Database credentials template.
 * Copy to db_credentials.php (same directory) and fill in real values.
 * db_credentials.php is gitignored — never commit real credentials.
 */
if (!defined('DB_HOST')) {
    define('DB_HOST', 'localhost');
}
if (!defined('DB_USER')) {
    define('DB_USER', 'tsaci_app');
}
if (!defined('DB_PASS')) {
    define('DB_PASS', 'your-db-password-here');
}
if (!defined('DB_NAME')) {
    define('DB_NAME', 'tsaci_cms');
}
